import MainPage from "@/views/shop/MainPage.vue";

const ShopRoutes = [
    {
        path: "/main",
        component: MainPage
        // meta: { requiresAuth: true }
    }
];

export default ShopRoutes;
